# V1.0 Chrome Core Bookmark-Manager Command Parity Implementation Plan

> **For agentic workers:** execute task-by-task with RED → GREEN. Never merge/tag/release from this plan. Use `feature/v1.0-stabilization`.

**Spec:** `docs/superpowers/specs/2026-09-22-v1.0-core-command-parity-design.md`

## Task 1 — Mixed selection becomes authoritative

- [x] RED: tests select Folder + Bookmark + Bookmark via Ctrl/Shift-equivalent ViewModel input and expect all rows retained in displayed order.
- [x] RED: Ctrl+A-equivalent all-displayed selection expects all displayed folders/bookmarks retained.
- [x] GREEN: refactor `UpdateSelectedContentItems` so folder presence does not collapse selection.
- [x] Keep legacy bookmark projections and single-item capability guards.
- [x] Verify navigation/search changes clear stale selection.
- [x] Full CI GREEN.

**Evidence — 2026-09-22**

- RED head: `21c0546`; Run #385 (`35716161774`) passed the first 39 safety/scale/contract steps, then full test failed **1 / 369** only on `MixedContentSelection_PreservesAllRowsInDisplayedOrder`: actual selection collapsed to the folder row.
- GREEN implementation: `f573345` (`feat: preserve authoritative mixed content selection`).
- Compatibility contract update: `d63ac88` keeps the single-item ambiguity safety invariant while checking the new mixed-selection guard.
- GREEN proof: Run #387 (`35716704315`) completed successfully with all **49 workflow steps** green, including full tests, safety/scale gates, publish, single-file verification, startup smoke, EXE version verification, owner kit packaging, and artifact upload.

## Task 2 — Generic mixed batch delete

- [x] RED: service test deletes bookmark + folder in one ordered request.
- [x] RED: ancestor+descendant input is canonicalized safely.
- [x] RED: protected root rejection and rollback.
- [x] RED: ViewModel Delete uses full selected node set.
- [x] RED: Undo/Redo restores exact mixed order, subtree counts, identity and dirty state.
- [x] GREEN: add generic `DeleteNodes` result/history path; preserve bookmark-only compatibility wrapper.
- [x] Update confirmation text/counts.
- [x] Full CI GREEN.

**Evidence — 2026-09-22**

- RED head: `57b6ceb`; Run #390 (`35717084429`) failed in stabilization tests because `DeleteNodes`, `CanDeleteSelectedContentItems`, and `DeleteSelectedContentItemsAsync` did not yet exist.
- GREEN application path: `82d8fba` → `3ccdc7c` added the generic delete contract/service, mixed folder-count projection, ViewModel capability, mixed delete command, history integration, and protected-root type guard.
- UI parity RED/GREEN: `47641c2` required mixed right-pane delete routing; `11a0d47` + `674d5cc` added a mixed-selection context command, Delete-key routing, canonicalized removal counts, and confirmation text covering selected items/bookmarks/folders.
- GREEN proof: Run #397 (`35720013444`) completed successfully through all normal Windows CI gates, including full tests, safety/scale verification, publish, single-file verification, startup smoke, EXE version verification, owner kit packaging, and artifact upload.

## Task 3 — Generic mixed batch move

- [x] RED: batch move accepts mixed nodes and preserves relative order.
- [x] RED: same-folder target-index normalization.
- [x] RED: folder cycle rejection happens before any mutation.
- [x] RED: Undo/Redo restores exact source/target orders.
- [x] GREEN: add generic `MoveNodes` application/history path; retain bookmark-only wrapper.
- [x] Full CI GREEN.

**Evidence — 2026-09-22**

- RED heads: `9639c92` / `ec905d4`; Run #399 (`35720214517`) failed as expected because `MoveNodes`, `CanMoveSelectedContentItems`, and `MoveSelectedContentItemsToEndAsync` were not implemented.
- GREEN service/history path: `34e6298` → `d5a36df` added indexed generic `MoveNodes`, duplicate/ancestor canonicalization, same-folder target-index normalization, pre-mutation folder-cycle rejection, atomic rollback, mixed-node batch history, and ViewModel routing while retaining the bookmark-only wrapper.
- UI parity: `9874a49` required mixed right-pane move routing; `549d337` + `23fb153` exposed and routed the mixed-selection Move command. `d152f14` fixed the final menu-state declaration ordering issue before final validation.
- GREEN proof: Run #406 (`35720568908`) completed successfully with all workflow steps green, including V1.0 stabilization tests, production/right-pane contracts, legacy V0.5–V0.9 compatibility gates, safety/scale suites, full tests, Windows publish, single-file verification, startup smoke, V1.0 EXE version verification, owner kit packaging, and artifact upload.

## Task 4 — Multi-node right-pane Drag & Drop

- [x] RED: selected-row drag payload contains ordered selected node set.
- [x] RED: unselected-row drag payload contains only that row.
- [x] RED: Before/After/Into/empty-space dispatches generic batch move.
- [x] RED: search positional DnD remains disabled.
- [x] GREEN: replace single-node payload with immutable ordered node payload.
- [x] Retire/update V0.6/V0.7 single-node mechanical contracts explicitly.
- [x] Full CI GREEN.

**Evidence — 2026-09-22**

- RED heads: `656a041` / `b4a77b5`; Runs #408–#410 failed as expected because `BookmarkDragPayload.ForContentRow`, immutable `Nodes`, and `MoveContentNodesAsync` were not yet implemented.
- Contract migration: `44983bf`, `e937e59`, and `b8355b4` explicitly retired the temporary V0.6/V0.7 single-node right-pane DnD invariant and required ordered multi-node payload/generic batch dispatch while preserving the search positional guard.
- GREEN core: `81cf5a5` → `a34c770` introduced immutable ordered payload capture, full-set drop validation, and indexed generic ViewModel batch movement.
- GREEN WPF routing: `e7f9f52` preserves the existing selection when dragging an already-selected row, drags only the pointer row when it is not selected, and routes Before/After/Into/empty-space through `MoveContentNodesAsync`; mixed payloads dropped on the folder tree are Into-only while single-folder tree drag keeps its legacy thirds behavior.
- GREEN proof: Run #413 (`35721221890`) completed successfully with all workflow steps green, including V1.0 production/right-pane contracts, updated V0.6/V0.7 DnD/delete contracts, safety/scale suites, full tests, Windows publish, single-file verification, startup smoke, V1.0 EXE version verification, owner kit packaging, and artifact upload.

## Task 5 — Open bookmark

- [ ] RED: folder Enter/double-click navigates; bookmark Enter/double-click invokes URL launcher.
- [ ] RED: http/https allowed; unsupported schemes rejected without shell execution.
- [ ] GREEN: introduce `IExternalUrlLauncher` and Windows implementation using shell URL handler.
- [ ] Add right-pane Open command and capability.
- [ ] Full CI GREEN.

## Task 6 — Clipboard Cut / Copy / Paste

- [ ] RED: subtree snapshot preserves mixed order and nested folders.
- [ ] RED: copy/paste allocates new IDs/GUIDs and leaves source intact.
- [ ] RED: same-document cut/paste moves originals only on successful paste.
- [ ] RED: failed paste leaves document unchanged.
- [ ] RED: Undo/Redo and dirty state.
- [ ] GREEN: introduce clipboard abstraction, structured payload, cloning/import service and UI commands.
- [ ] Add Ctrl+X/C/V and context menu commands.
- [ ] Full CI GREEN.

## Task 7 — Sort by name

- [ ] RED: folders sort before bookmarks; each group sorts by culture-aware title.
- [ ] RED: stable tie handling, no-op behavior, protected-root rules.
- [ ] RED: Undo/Redo exact order.
- [ ] GREEN: add sort service/history/ViewModel command and menu action.
- [ ] Full CI GREEN.

## Task 8 — Search folders and bookmarks

- [ ] RED: title-matching folders appear in all-bookmarks search.
- [ ] RED: current-folder scope matches direct folders + bookmarks only.
- [ ] RED: bookmark title/URL matching remains intact.
- [ ] RED: result navigation enters folder or navigates to bookmark owner correctly.
- [ ] RED: search-mode positional DnD remains disabled.
- [ ] GREEN: generalize search index/result types to `BookmarkNode`.
- [ ] Full CI GREEN.

## Task 9 — Bookmark HTML export

- [ ] RED: representative nested mixed tree exports deterministic Netscape bookmark HTML.
- [ ] RED: HTML escaping and Unicode.
- [ ] RED: export does not dirty or mutate document.
- [ ] GREEN: implement bounded streaming writer and SaveFileDialog UI path.
- [ ] Full CI GREEN.

## Task 10 — Bookmark HTML import

- [ ] RED: nested exported fixture imports with exact names/URLs/order.
- [ ] RED: IDs/GUIDs are newly allocated and unique.
- [ ] RED: malformed/deep/oversized input fails atomically.
- [ ] RED: successful import is one undoable operation.
- [ ] GREEN: implement bounded Netscape bookmark HTML parser + import service.
- [ ] Full CI GREEN.

## Task 11 — Command/menu/shortcut parity contract

- [ ] Add/update mechanical UI contract covering Open, Cut, Copy, Paste, Sort, Import, Export, mixed Delete, and mixed selection.
- [ ] Verify Ctrl+A / Escape / Enter / Ctrl+X/C/V routing.
- [ ] Verify no code-behind hierarchy mutation.
- [ ] Full CI GREEN.

## Task 12 — Re-run parity audit and release gates

- [ ] Re-check current Chrome help + Chromium behavior.
- [ ] Mark each audit row MATCH / INTENTIONALLY SAFER / BROWSER-ONLY / NOT APPLICABLE / PARITY GAP.
- [ ] No core local PARITY GAP may remain.
- [ ] Run full normal Windows CI.
- [ ] Run full `Invoke-V10ReleaseReadiness.ps1` with 250000 URLs / 4000 folders / 100000 history edits.
- [ ] Generate exact post-parity RC.
- [ ] Restart owner acceptance from Step 1.
- [ ] Only after owner acceptance: resume stable-release Tasks 11–13.
