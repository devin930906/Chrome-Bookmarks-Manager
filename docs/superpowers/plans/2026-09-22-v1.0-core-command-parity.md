# V1.0 Chrome Core Bookmark-Manager Command Parity Implementation Plan

> **For agentic workers:** execute task-by-task with RED → GREEN. Never merge/tag/release from this plan. Use `feature/v1.0-stabilization`.

**Spec:** `docs/superpowers/specs/2026-09-22-v1.0-core-command-parity-design.md`

## Task 1 — Mixed selection becomes authoritative

- [x] RED: tests select Folder + Bookmark + Bookmark via Ctrl/Shift-equivalent ViewModel input and expect all rows retained in displayed order.
- [x] RED: Ctrl+A-equivalent all-displayed selection expects all displayed folders/bookmarks retained.
- [x] GREEN: refactor `UpdateSelectedContentItems` so folder presence does not collapse selection.
- [x] Keep legacy bookmark projections and single-item capability guards.
- [x] Verify navigation/search changes clear stale selection.
- [x] Full CI GREEN.

**Evidence — 2026-09-22**

- RED head: `21c0546`; Run #385 (`35716161774`) passed the first 39 safety/scale/contract steps, then full test failed **1 / 369** only on `MixedContentSelection_PreservesAllRowsInDisplayedOrder`: actual selection collapsed to the folder row.
- GREEN implementation: `f573345` (`feat: preserve authoritative mixed content selection`).
- Compatibility contract update: `d63ac88` keeps the single-item ambiguity safety invariant while checking the new mixed-selection guard.
- GREEN proof: Run #387 (`35716704315`) completed successfully with all **49 workflow steps** green, including full tests, safety/scale gates, publish, single-file verification, startup smoke, EXE version verification, owner kit packaging, and artifact upload.

## Task 2 — Generic mixed batch delete

- [x] RED: service test deletes bookmark + folder in one ordered request.
- [x] RED: ancestor+descendant input is canonicalized safely.
- [x] RED: protected root rejection and rollback.
- [x] RED: ViewModel Delete uses full selected node set.
- [x] RED: Undo/Redo restores exact mixed order, subtree counts, identity and dirty state.
- [x] GREEN: add generic `DeleteNodes` result/history path; preserve bookmark-only compatibility wrapper.
- [x] Update confirmation text/counts.
- [x] Full CI GREEN.

**Evidence — 2026-09-22**

- RED head: `57b6ceb`; Run #390 (`35717084429`) failed in stabilization tests because `DeleteNodes`, `CanDeleteSelectedContentItems`, and `DeleteSelectedContentItemsAsync` did not yet exist.
- GREEN application path: `82d8fba` → `3ccdc7c` added the generic delete contract/service, mixed folder-count projection, ViewModel capability, mixed delete command, history integration, and protected-root type guard.
- UI parity RED/GREEN: `47641c2` required mixed right-pane delete routing; `11a0d47` + `674d5cc` added a mixed-selection context command, Delete-key routing, canonicalized removal counts, and confirmation text covering selected items/bookmarks/folders.
- GREEN proof: Run #397 (`35720013444`) completed successfully through all normal Windows CI gates, including full tests, safety/scale verification, publish, single-file verification, startup smoke, EXE version verification, owner kit packaging, and artifact upload.

## Task 3 — Generic mixed batch move

- [x] RED: batch move accepts mixed nodes and preserves relative order.
- [x] RED: same-folder target-index normalization.
- [x] RED: folder cycle rejection happens before any mutation.
- [x] RED: Undo/Redo restores exact source/target orders.
- [x] GREEN: add generic `MoveNodes` application/history path; retain bookmark-only wrapper.
- [x] Full CI GREEN.

**Evidence — 2026-09-22**

- RED heads: `9639c92` / `ec905d4`; Run #399 (`35720214517`) failed as expected because `MoveNodes`, `CanMoveSelectedContentItems`, and `MoveSelectedContentItemsToEndAsync` were not implemented.
- GREEN service/history path: `34e6298` → `d5a36df` added indexed generic `MoveNodes`, duplicate/ancestor canonicalization, same-folder target-index normalization, pre-mutation folder-cycle rejection, atomic rollback, mixed-node batch history, and ViewModel routing while retaining the bookmark-only wrapper.
- UI parity: `9874a49` required mixed right-pane move routing; `549d337` + `23fb153` exposed and routed the mixed-selection Move command. `d152f14` fixed the final menu-state declaration ordering issue before final validation.
- GREEN proof: Run #406 (`35720568908`) completed successfully with all workflow steps green, including V1.0 stabilization tests, production/right-pane contracts, legacy V0.5–V0.9 compatibility gates, safety/scale suites, full tests, Windows publish, single-file verification, startup smoke, V1.0 EXE version verification, owner kit packaging, and artifact upload.

## Task 4 — Multi-node right-pane Drag & Drop

- [x] RED: selected-row drag payload contains ordered selected node set.
- [x] RED: unselected-row drag payload contains only that row.
- [x] RED: Before/After/Into/empty-space dispatches generic batch move.
- [x] RED: search positional DnD remains disabled.
- [x] GREEN: replace single-node payload with immutable ordered node payload.
- [x] Retire/update V0.6/V0.7 single-node mechanical contracts explicitly.
- [x] Full CI GREEN.

**Evidence — 2026-09-22**

- RED heads: `656a041` / `b4a77b5`; Runs #408–#410 failed as expected because `BookmarkDragPayload.ForContentRow`, immutable `Nodes`, and `MoveContentNodesAsync` were not yet implemented.
- Contract migration: `44983bf`, `e937e59`, and `b8355b4` explicitly retired the temporary V0.6/V0.7 single-node right-pane DnD invariant and required ordered multi-node payload/generic batch dispatch while preserving the search positional guard.
- GREEN core: `81cf5a5` → `a34c770` introduced immutable ordered payload capture, full-set drop validation, and indexed generic ViewModel batch movement.
- GREEN WPF routing: `e7f9f52` preserves the existing selection when dragging an already-selected row, drags only the pointer row when it is not selected, and routes Before/After/Into/empty-space through `MoveContentNodesAsync`; mixed payloads dropped on the folder tree are Into-only while single-folder tree drag keeps its legacy thirds behavior.
- GREEN proof: Run #413 (`35721221890`) completed successfully with all workflow steps green, including V1.0 production/right-pane contracts, updated V0.6/V0.7 DnD/delete contracts, safety/scale suites, full tests, Windows publish, single-file verification, startup smoke, V1.0 EXE version verification, owner kit packaging, and artifact upload.

## Task 5 — Open bookmark

- [x] RED: folder Enter/double-click navigates; bookmark Enter/double-click invokes URL launcher.
- [x] RED: http/https allowed; unsupported schemes rejected without shell execution.
- [x] GREEN: introduce `IExternalUrlLauncher` and Windows implementation using shell URL handler.
- [x] Add right-pane Open command and capability.
- [x] Full CI GREEN.

**Evidence — 2026-09-22**

- RED tests: `bf515fd`, `1516d96`, `04fd8d3`, and `dd2c3b5` required folder navigation, bookmark launching, safe http/https-only shell execution, and the right-pane Open command.
- GREEN implementation: `3a05528` → `6c9aeec` added the launcher contract/errors, Windows shell implementation, ViewModel Open capability/command, context-menu routing, Enter/double-click behavior, and production launcher wiring.
- Syntax correction: `c3ab397` fixed the unsupported-scheme diagnostic string so the WPF project compiled.
- Right-pane parity correction: `e9670e5` made folder navigation explicit in `MainWindow.xaml.cs` while bookmark rows continue through `OpenSelectedContentItemAsync`.
- GREEN proof: Run #424 (`35723859149`) completed successfully with all workflow steps green, including stabilization tests, repository safety, normal CI/version/UI/right-pane contracts, release asset/measurement/workflow contracts, all legacy UI/safety/scale gates, full solution tests, Windows publish, single-file verification, startup smoke, V1.0 EXE version verification, owner-kit packaging, and artifact upload.

## Task 6 — Clipboard Cut / Copy / Paste

- [x] RED: subtree snapshot preserves mixed order and nested folders.
- [x] RED: copy/paste allocates new IDs/GUIDs and leaves source intact.
- [x] RED: same-document cut/paste moves originals only on successful paste.
- [x] RED: failed paste leaves document unchanged.
- [x] RED: Undo/Redo and dirty state.
- [x] GREEN: introduce clipboard abstraction, structured payload, cloning/import service and UI commands.
- [x] Add Ctrl+X/C/V and context menu commands.
- [x] Full CI GREEN.

**Evidence — 2026-09-22**

- RED service contract: `76c9760` required structured copy/cut snapshot semantics, including mixed nested subtree order, fresh copy identities, same-document cut identity, and atomic failed paste.
- GREEN clipboard service: `b9f31cc` added the structured clipboard payload/service; `842e687` corrected the synthetic fixture constructor without weakening the required behavior.
- RED ViewModel/history contract: `ca4b8e5` required Copy/Cut to remain clean until Paste, successful Paste to become one history step, Cut clipboard consumption only after success, and exact Undo/Redo identity/count semantics.
- GREEN ViewModel/history: `c4e7bdc` added the clipboard store contract, paste history entry, capabilities, and ViewModel Copy/Cut/Paste routing. `062e29d` corrected the ViewModel test's nested-subtree count expectations to match the already-green service contract of +3 URLs / +2 folders.
- Production UI: `8b49045` added the in-memory production clipboard store, `6e81ee1` exposed production dependency injection, `68829e1` added right-pane Cut/Copy/Paste commands, and `44be36e` wired Ctrl+X/C/V, handlers, errors, and the production clipboard service/store.
- GREEN proof: Run #436 (`35726339661`) completed successfully through all Windows CI gates, including stabilization tests, production/right-pane contracts, legacy UI/safety/scale regression suites, full tests, Windows publish, single-file verification, startup smoke, V1.0 EXE version verification, owner-kit packaging, and artifact upload.

## Task 7 — Sort by name

- [x] RED: folders sort before bookmarks; each group sorts by culture-aware title.
- [x] RED: stable tie handling, no-op behavior, protected-root rules.
- [x] RED: Undo/Redo exact order.
- [x] GREEN: add sort service/history/ViewModel command and menu action.
- [x] Full CI GREEN.

**Evidence — 2026-09-22**

- RED head: `68208e3`; Run #438 (`35726724771`) failed as expected because `ChromeBookmarksManager.Application.Sorting` did not yet exist.
- GREEN implementation: `84799c5` → `25d2164` added sort result/service/history, current-folder capability, culture-aware folders-first stable ordering, no-op semantics, and Undo/Redo dirty-state integration without rebuilding the search index.
- GREEN proof: Run #440 (`35728265422`) completed successfully with the Windows CI job green and no failed steps.

## Task 8 — Search folders and bookmarks

- [x] RED: title-matching folders appear in all-bookmarks search.
- [x] RED: current-folder scope matches direct folders + bookmarks only.
- [x] RED: bookmark title/URL matching remains intact.
- [x] RED: result navigation enters folder or navigates to bookmark owner correctly.
- [x] RED: search-mode positional DnD remains disabled.
- [x] GREEN: generalize search index/result types to `BookmarkNode`.
- [x] Full CI GREEN.

**Evidence — 2026-09-22**

- RED head: `4b43c7d`; Run #442 (`35729627914`) failed as expected because `BookmarkSearchIndex.Nodes` did not yet exist.
- GREEN core: `b6b64d0` → `a38aad6` generalized the index/service/ViewModel result path to original `BookmarkNode` references, added folder-title matching, kept bookmark title/URL matching, preserved bookmark-count compatibility projections, and added folder-aware search navigation.
- UI activation correction: `3e6e345` restored search-mode Enter/double-click navigation precedence while ordinary browse mode continues using Open behavior.
- Contract migration: `e9ce684` → `99f213a` migrated legacy search stubs and bookmark-only assertions to the mixed-node result contract without weakening bookmark-specific tests.
- GREEN proof: Run #459 (`35730593573`) completed successfully with no failed steps, including stabilization tests, repository safety, right-pane parity, build, search UI contract, browser/search/move/delete/history/write scale gates, full tests, Windows x64 publish, single-file verification, startup smoke, V1.0 EXE version verification, owner-kit packaging, and artifact upload.

## Task 9 — Bookmark HTML export

- [x] RED: representative nested mixed tree exports deterministic Netscape bookmark HTML.
- [x] RED: HTML escaping and Unicode.
- [x] RED: export does not dirty or mutate document.
- [x] GREEN: implement bounded streaming writer and SaveFileDialog UI path.
- [x] Full CI GREEN.

**Evidence — 2026-09-22**

- Original RED: `4040711` required deterministic nested Netscape HTML, HTML escaping/Unicode preservation, and read-only ViewModel export semantics.
- Initial GREEN path: `80d9619` → `841197c` added the bounded iterative streaming exporter, read-only ViewModel command, File-menu action, SaveFileDialog, and UTF-8 HTML output.
- Safety-boundary regression: Run #468 (`35731581168`) showed that the initial UI implementation violated the existing V0.6 ownership invariant because `MainWindow.xaml.cs` directly created a write stream/writer.
- Regression RED: `b2b0bdc`; Run #469 (`35732842065`) failed exactly because `BookmarkHtmlExportService.ExportFileAsync` did not yet exist.
- Architecture fix: `fad6fa6` → `19f8655` moved file creation/writing into `Application/Exporting`, kept SaveFileDialog in the UI, and routed the selected path through the ViewModel without weakening `Verify-V06MoveSafety.ps1`. `259746b` supplied the final UTF-8 encoding import.
- GREEN proof: Run #473 (`35733407525`) completed successfully through all normal Windows CI gates, including the new export test, repository/UI/right-pane contracts, V0.6 move safety, all scale/safety suites, full tests, Windows x64 publish, single-file verification, startup smoke, V1.0 EXE version verification, owner-kit packaging, and artifact upload.

## Task 10 — Bookmark HTML import

- [ ] RED: nested exported fixture imports with exact names/URLs/order.
- [ ] RED: IDs/GUIDs are newly allocated and unique.
- [ ] RED: malformed/deep/oversized input fails atomically.
- [ ] RED: successful import is one undoable operation.
- [ ] GREEN: implement bounded Netscape bookmark HTML parser + import service.
- [ ] Full CI GREEN.

## Task 11 — Command/menu/shortcut parity contract

- [ ] Add/update mechanical UI contract covering Open, Cut, Copy, Paste, Sort, Import, Export, mixed Delete, and mixed selection.
- [ ] Verify Ctrl+A / Escape / Enter / Ctrl+X/C/V routing.
- [ ] Verify no code-behind hierarchy mutation.
- [ ] Full CI GREEN.

## Task 12 — Re-run parity audit and release gates

- [ ] Re-check current Chrome help + Chromium behavior.
- [ ] Mark each audit row MATCH / INTENTIONALLY SAFER / BROWSER-ONLY / NOT APPLICABLE / PARITY GAP.
- [ ] No core local PARITY GAP may remain.
- [ ] Run full normal Windows CI.
- [ ] Run full `Invoke-V10ReleaseReadiness.ps1` with 250000 URLs / 4000 folders / 100000 history edits.
- [ ] Generate exact post-parity RC.
- [ ] Restart owner acceptance from Step 1.
- [ ] Only after owner acceptance: resume stable-release Tasks 11–13.
